apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

resources:
  - redis-local.yaml

# Local specific overrides
patchesStrategicMerge:
  - nginx-proxy-lb-patch.yaml
  - backend-lb-patch.yaml